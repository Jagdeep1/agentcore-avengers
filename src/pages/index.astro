---
/**
 * Landing Page - The AgentCore Initiative
 * Mission Control themed course overview
 */

import HomeLayout from '@/layouts/HomeLayout.astro';
import DayCard from '@/components/course/DayCard.astro';

// Show all days on homepage, but only Day 1 is accessible in production
const days = [
  {
    dayNumber: 1,
    title: 'Day 1',
    avengersTitle: 'The First Avenger',
    focus: 'Foundation - Strands Agent + AgentCore Runtime',
  },
  {
    dayNumber: 2,
    title: 'Day 2',
    avengersTitle: 'Age of Ultron',
    focus: 'Identity + AgentCore Gateway',
  },
  {
    dayNumber: 3,
    title: 'Day 3',
    avengersTitle: 'Infinity War',
    focus: 'Memory + Tools',
  },
  {
    dayNumber: 4,
    title: 'Day 4',
    avengersTitle: 'Endgame',
    focus: 'Guardrails + Observability',
  },
  {
    dayNumber: 5,
    title: 'Day 5',
    avengersTitle: 'Assemble',
    focus: 'Multi-agent Capstone',
  },
];
const base = import.meta.env.BASE_URL;

const prerequisites = [
  'AWS account (you own the bill)',
  'Python 3.10+ installed',
  'Basic understanding of AI agents (LangChain, OpenAI, etc.)',
  'Familiarity with command line tools',
  'Code editor (VS Code recommended)',
];
---

<HomeLayout>
  <!-- Hero Section -->
  <section class="hero">
    <!-- Animated background -->
    <div class="hero__bg">
      <div class="hero__grid"></div>
      <div class="hero__glow hero__glow--1"></div>
      <div class="hero__glow hero__glow--2"></div>
      <div class="hero__glow hero__glow--3"></div>
      <div class="hero__scanline"></div>
    </div>

    <div class="hero__content">
      <!-- Status badge -->
      <div class="hero__status animate-item" style="--delay: 0">
        <span class="hero__status-dot"></span>
        <span>SYSTEM ONLINE</span>
        <span class="hero__status-divider">|</span>
        <span>5-DAY MISSION</span>
      </div>

      <!-- Main title -->
      <h1 class="hero__title animate-item" style="--delay: 1">
        <span class="hero__title-line">THE</span>
        <span class="hero__title-main">
          <span class="hero__title-agent">AGENT</span><span class="hero__title-core">CORE</span>
        </span>
        <span class="hero__title-line">INITIATIVE</span>
      </h1>

      <!-- Tagline -->
      <p class="hero__tagline animate-item" style="--delay: 2">
        Build production-ready AI agents with
        <span class="hero__highlight">AWS AgentCore</span>
      </p>

      <!-- CTA buttons -->
      <div class="hero__actions animate-item" style="--delay: 3">
        <a href={`${base}days/1`} class="hero__cta hero__cta--primary">
          <span class="hero__cta-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polygon points="5 3 19 12 5 21 5 3" />
            </svg>
          </span>
          Begin Mission
        </a>
        <a href="#overview" class="hero__cta hero__cta--secondary">
          View Briefing
        </a>
      </div>

      <!-- Tech stack badges -->
      <div class="hero__tech animate-item" style="--delay: 4">
        <span class="hero__tech-label">POWERED BY</span>
        <div class="hero__tech-badges">
          <span class="hero__tech-badge">AWS AgentCore</span>
          <span class="hero__tech-badge">Strands SDK</span>
          <span class="hero__tech-badge">Python</span>
          <span class="hero__tech-badge">CDK</span>
        </div>
      </div>
    </div>

    <!-- Animated visual element -->
    <div class="hero__visual animate-item" style="--delay: 2">
      <div class="hero__reactor">
        <div class="hero__reactor-ring hero__reactor-ring--1"></div>
        <div class="hero__reactor-ring hero__reactor-ring--2"></div>
        <div class="hero__reactor-ring hero__reactor-ring--3"></div>
        <div class="hero__reactor-core"></div>
        <div class="hero__reactor-pulse"></div>
      </div>
      <!-- Connection lines to days -->
      <svg class="hero__connections" viewBox="0 0 400 400" preserveAspectRatio="xMidYMid meet">
        <defs>
          <linearGradient id="line-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#3b82f6" stop-opacity="0.8" />
            <stop offset="100%" stop-color="#3b82f6" stop-opacity="0" />
          </linearGradient>
        </defs>
        <circle cx="200" cy="200" r="80" fill="none" stroke="url(#line-gradient)" stroke-width="1" stroke-dasharray="4 4" class="orbit-line" />
        <circle cx="200" cy="200" r="120" fill="none" stroke="url(#line-gradient)" stroke-width="1" stroke-dasharray="4 4" class="orbit-line" style="animation-delay: -2s" />
        <circle cx="200" cy="200" r="160" fill="none" stroke="url(#line-gradient)" stroke-width="1" stroke-dasharray="4 4" class="orbit-line" style="animation-delay: -4s" />
      </svg>
    </div>

    <!-- Scroll indicator -->
    <div class="hero__scroll animate-item" style="--delay: 5">
      <span>Scroll to explore</span>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 5v14M5 12l7 7 7-7" />
      </svg>
    </div>
  </section>

  <!-- Course Overview Section -->
  <section id="overview" class="overview">
    <div class="overview__container">
      <div class="section-header">
        <span class="section-tag">MISSION BRIEFING</span>
        <h2 class="section-title">Your 5-Day Journey</h2>
        <p class="section-desc">
          Transform from agent hobbyist to production engineer. Each day builds on the last,
          introducing controlled disasters that reveal why enterprise features matter.
        </p>
      </div>

      <!-- Timeline visualization -->
      <div class="timeline">
        {days.map((day, index) => (
          <div class="timeline__item" style={`--index: ${index}`}>
            <div class="timeline__marker" style={`--color: var(--color-day-${day.dayNumber})`}>
              <span class="timeline__number">{day.dayNumber}</span>
            </div>
            <div class="timeline__content">
              <span class="timeline__label">{day.avengersTitle}</span>
              <span class="timeline__focus">{day.focus}</span>
            </div>
            {index < days.length - 1 && <div class="timeline__connector" />}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- What You'll Build Section -->
  <section class="capstone">
    <div class="capstone__container">
      <div class="section-header">
        <span class="section-tag">FINAL OBJECTIVE</span>
        <h2 class="section-title">What You'll Build</h2>
        <p class="section-desc">
          A production-ready multi-agent data pipeline combining everything you learn.
        </p>
      </div>

      <div class="capstone__visual">
        <div class="capstone__diagram">
          <!-- Agent nodes -->
          <div class="capstone__node capstone__node--extract">
            <div class="capstone__node-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
                <polyline points="17 8 12 3 7 8" />
                <line x1="12" y1="3" x2="12" y2="15" />
              </svg>
            </div>
            <span class="capstone__node-label">Extract Agent</span>
            <span class="capstone__node-desc">Synthetic Data Generation</span>
          </div>

          <div class="capstone__arrow">
            <svg viewBox="0 0 48 24" fill="none">
              <path d="M0 12h40M32 4l8 8-8 8" stroke="currentColor" stroke-width="2" />
            </svg>
          </div>

          <div class="capstone__node capstone__node--transform">
            <div class="capstone__node-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            </div>
            <span class="capstone__node-label">Transform Agent</span>
            <span class="capstone__node-desc">Data Processing</span>
          </div>

          <div class="capstone__arrow">
            <svg viewBox="0 0 48 24" fill="none">
              <path d="M0 12h40M32 4l8 8-8 8" stroke="currentColor" stroke-width="2" />
            </svg>
          </div>

          <div class="capstone__node capstone__node--report">
            <div class="capstone__node-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                <polyline points="14 2 14 8 20 8" />
                <line x1="16" y1="13" x2="8" y2="13" />
                <line x1="16" y1="17" x2="8" y2="17" />
              </svg>
            </div>
            <span class="capstone__node-label">Report Agent</span>
            <span class="capstone__node-desc">Analytics Output</span>
          </div>
        </div>

        <!-- Features list -->
        <div class="capstone__features">
          <div class="capstone__feature">
            <span class="capstone__feature-icon">✓</span>
            <span>AgentCore Runtime deployment</span>
          </div>
          <div class="capstone__feature">
            <span class="capstone__feature-icon">✓</span>
            <span>Identity & Gateway security</span>
          </div>
          <div class="capstone__feature">
            <span class="capstone__feature-icon">✓</span>
            <span>Persistent Memory integration</span>
          </div>
          <div class="capstone__feature">
            <span class="capstone__feature-icon">✓</span>
            <span>Knowledge Bases for context</span>
          </div>
          <div class="capstone__feature">
            <span class="capstone__feature-icon">✓</span>
            <span>Full observability & guardrails</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Prerequisites Section -->
  <section class="prerequisites">
    <div class="prerequisites__container">
      <div class="section-header section-header--left">
        <span class="section-tag">REQUIREMENTS</span>
        <h2 class="section-title">Prerequisites</h2>
        <p class="section-desc">
          Make sure you have these ready before starting your mission.
        </p>
      </div>

      <div class="prerequisites__grid">
        {prerequisites.map((prereq, index) => (
          <div class="prereq-item" style={`--index: ${index}`}>
            <div class="prereq-item__check">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </div>
            <span class="prereq-item__text">{prereq}</span>
          </div>
        ))}
      </div>

      <div class="prerequisites__note">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10" />
          <line x1="12" y1="16" x2="12" y2="12" />
          <line x1="12" y1="8" x2="12.01" y2="8" />
        </svg>
        <span>Day 1 includes complete environment setup instructions</span>
      </div>
    </div>
  </section>

  <!-- Day Selection Grid -->
  <section class="days-grid">
    <div class="days-grid__container">
      <div class="section-header">
        <span class="section-tag">SELECT MISSION</span>
        <h2 class="section-title">Choose Your Day</h2>
        <p class="section-desc">
          Complete each day sequentially to unlock the next mission.
        </p>
      </div>

      <div class="days-grid__cards">
        {days.map((day) => (
          <DayCard
            dayNumber={day.dayNumber}
            title={day.title}
            avengersTitle={day.avengersTitle}
            focus={day.focus}
          />
        ))}
      </div>
    </div>
  </section>

  <!-- Final CTA Section -->
  <section class="final-cta">
    <div class="final-cta__container">
      <div class="final-cta__content">
        <h2 class="final-cta__title">Ready to Begin?</h2>
        <p class="final-cta__desc">
          Start your journey to production-ready AI agents.
          Day 1 takes approximately 3-4 hours.
        </p>
        <a href={`${base}days/1`} class="final-cta__button">
          <span class="final-cta__button-glow"></span>
          <span class="final-cta__button-text">
            Launch Day 1: The First Avenger
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7" />
            </svg>
          </span>
        </a>
      </div>
    </div>
  </section>
</HomeLayout>

<style>
  /* ============================================
     HERO SECTION
     ============================================ */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 6rem var(--space-content) 4rem;
    overflow: hidden;
  }

  /* Animated background */
  .hero__bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }

  .hero__grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(59, 130, 246, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(59, 130, 246, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    animation: grid-move 20s linear infinite;
  }

  @keyframes grid-move {
    0% { transform: translate(0, 0); }
    100% { transform: translate(60px, 60px); }
  }

  .hero__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    animation: glow-pulse 8s ease-in-out infinite;
  }

  .hero__glow--1 {
    top: 10%;
    left: 20%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.15) 0%, transparent 70%);
  }

  .hero__glow--2 {
    bottom: 20%;
    right: 15%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(139, 92, 246, 0.1) 0%, transparent 70%);
    animation-delay: -3s;
  }

  .hero__glow--3 {
    top: 40%;
    right: 30%;
    width: 250px;
    height: 250px;
    background: radial-gradient(circle, rgba(16, 185, 129, 0.08) 0%, transparent 70%);
    animation-delay: -5s;
  }

  @keyframes glow-pulse {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
  }

  .hero__scanline {
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(59, 130, 246, 0.015) 2px,
      rgba(59, 130, 246, 0.015) 4px
    );
    pointer-events: none;
  }

  /* Hero content */
  .hero__content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
  }

  /* Animation for items */
  .animate-item {
    opacity: 0;
    transform: translateY(30px);
    animation: fade-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: calc(var(--delay) * 0.15s);
  }

  @keyframes fade-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Status badge */
  .hero__status {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 1.25rem;
    background: rgba(59, 130, 246, 0.1);
    border: 1px solid rgba(59, 130, 246, 0.2);
    border-radius: 9999px;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--color-accent-primary);
    margin-bottom: 2rem;
  }

  .hero__status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--color-accent-success);
    box-shadow: 0 0 12px var(--color-accent-success);
    animation: pulse-dot 2s ease-in-out infinite;
  }

  @keyframes pulse-dot {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  .hero__status-divider {
    opacity: 0.3;
  }

  /* Title */
  .hero__title {
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.03em;
    margin-bottom: 1.5rem;
  }

  .hero__title-line {
    display: block;
    font-size: 0.35em;
    font-weight: 600;
    letter-spacing: 0.3em;
    color: var(--color-text-muted);
    margin-bottom: 0.25em;
  }

  .hero__title-line:last-child {
    margin-top: 0.25em;
    margin-bottom: 0;
  }

  .hero__title-main {
    display: block;
  }

  .hero__title-agent {
    color: var(--color-text-primary);
  }

  .hero__title-core {
    color: var(--color-accent-primary);
    text-shadow: 0 0 40px rgba(59, 130, 246, 0.5);
  }

  /* Tagline */
  .hero__tagline {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    margin-bottom: 2.5rem;
    line-height: 1.6;
  }

  .hero__highlight {
    color: var(--color-accent-primary);
    font-weight: 600;
  }

  /* CTA buttons */
  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .hero__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hero__cta--primary {
    background: linear-gradient(135deg, var(--color-accent-primary) 0%, #2563eb 100%);
    color: white;
    box-shadow:
      0 4px 20px rgba(59, 130, 246, 0.4),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .hero__cta--primary:hover {
    transform: translateY(-2px);
    box-shadow:
      0 8px 30px rgba(59, 130, 246, 0.5),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .hero__cta-icon {
    width: 1.25rem;
    height: 1.25rem;
  }

  .hero__cta--secondary {
    background: transparent;
    color: var(--color-text-secondary);
    border: 1px solid var(--border-default);
  }

  .hero__cta--secondary:hover {
    background: var(--color-bg-elevated);
    color: var(--color-text-primary);
    border-color: var(--border-strong);
  }

  /* Tech badges */
  .hero__tech {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .hero__tech-label {
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--color-text-muted);
  }

  .hero__tech-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
  }

  .hero__tech-badge {
    padding: 0.375rem 0.875rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-text-secondary);
    background: var(--color-bg-elevated);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-md);
  }

  /* Visual element */
  .hero__visual {
    position: absolute;
    top: 50%;
    right: 5%;
    transform: translateY(-50%);
    width: 400px;
    height: 400px;
    opacity: 0.6;
    pointer-events: none;
  }

  @media (max-width: 1200px) {
    .hero__visual {
      display: none;
    }
  }

  .hero__reactor {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 120px;
    height: 120px;
  }

  .hero__reactor-ring {
    position: absolute;
    inset: 0;
    border: 2px solid rgba(59, 130, 246, 0.3);
    border-radius: 50%;
    animation: rotate-ring 10s linear infinite;
  }

  .hero__reactor-ring--1 {
    inset: -20px;
    border-style: dashed;
    animation-direction: reverse;
  }

  .hero__reactor-ring--2 {
    inset: -50px;
    border-width: 1px;
    animation-duration: 15s;
  }

  .hero__reactor-ring--3 {
    inset: -80px;
    border-width: 1px;
    border-style: dotted;
    animation-duration: 20s;
    animation-direction: reverse;
  }

  @keyframes rotate-ring {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .hero__reactor-core {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40px;
    height: 40px;
    background: radial-gradient(circle, var(--color-accent-primary) 0%, transparent 70%);
    border-radius: 50%;
    box-shadow: 0 0 60px var(--color-accent-primary);
  }

  .hero__reactor-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 2px solid var(--color-accent-primary);
    animation: pulse-ring 2s ease-out infinite;
  }

  @keyframes pulse-ring {
    0% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 1;
    }
    100% {
      transform: translate(-50%, -50%) scale(3);
      opacity: 0;
    }
  }

  .hero__connections {
    position: absolute;
    inset: 0;
  }

  .orbit-line {
    animation: rotate-orbit 30s linear infinite;
    transform-origin: center;
  }

  @keyframes rotate-orbit {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  /* Scroll indicator */
  .hero__scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .hero__scroll svg {
    width: 1.25rem;
    height: 1.25rem;
    animation: bounce-arrow 2s ease-in-out infinite;
  }

  @keyframes bounce-arrow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(5px); }
  }

  /* ============================================
     SECTION COMMON STYLES
     ============================================ */
  .section-header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto 4rem;
  }

  .section-header--left {
    text-align: left;
    margin-left: 0;
  }

  .section-tag {
    display: inline-block;
    padding: 0.375rem 1rem;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--color-accent-primary);
    background: rgba(59, 130, 246, 0.1);
    border: 1px solid rgba(59, 130, 246, 0.2);
    border-radius: var(--radius-md);
    margin-bottom: 1.5rem;
  }

  .section-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
  }

  .section-desc {
    font-size: 1.0625rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* ============================================
     OVERVIEW / TIMELINE SECTION
     ============================================ */
  .overview {
    padding: var(--space-section) var(--space-content);
    background: var(--color-bg-secondary);
  }

  .overview__container {
    max-width: 1000px;
    margin: 0 auto;
  }

  .timeline {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  @media (min-width: 768px) {
    .timeline {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }
  }

  .timeline__item {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    flex: 1;
    padding: 1rem;
    opacity: 0;
    animation: fade-up 0.6s ease forwards;
    animation-delay: calc(var(--index) * 0.1s + 0.3s);
  }

  .timeline__marker {
    width: 3.5rem;
    height: 3.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, color-mix(in srgb, var(--color) 20%, transparent), transparent);
    border: 2px solid var(--color);
    border-radius: 50%;
    margin-bottom: 1rem;
    box-shadow: 0 0 20px color-mix(in srgb, var(--color) 30%, transparent);
  }

  .timeline__number {
    font-family: var(--font-mono);
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color);
  }

  .timeline__content {
    text-align: center;
  }

  .timeline__label {
    display: block;
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: 0.25rem;
  }

  .timeline__focus {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
  }

  .timeline__connector {
    display: none;
  }

  @media (min-width: 768px) {
    .timeline__connector {
      display: block;
      position: absolute;
      top: 1.75rem;
      left: calc(50% + 2rem);
      width: calc(100% - 4rem);
      height: 2px;
      background: linear-gradient(90deg, var(--color-day-1), transparent);
      opacity: 0.3;
    }
  }

  /* ============================================
     CAPSTONE SECTION
     ============================================ */
  .capstone {
    padding: var(--space-section) var(--space-content);
  }

  .capstone__container {
    max-width: 1000px;
    margin: 0 auto;
  }

  .capstone__visual {
    background: var(--color-bg-secondary);
    border: var(--border-subtle);
    border-radius: var(--radius-xl);
    padding: 3rem 2rem;
  }

  .capstone__diagram {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .capstone__diagram {
      flex-direction: row;
      justify-content: center;
      gap: 1rem;
    }
  }

  .capstone__node {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1.5rem 2rem;
    background: var(--color-bg-primary);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-lg);
    text-align: center;
    min-width: 160px;
    transition: all 0.3s ease;
  }

  .capstone__node:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-elevated);
  }

  .capstone__node--extract {
    border-color: rgba(59, 130, 246, 0.3);
  }

  .capstone__node--extract:hover {
    box-shadow: 0 10px 40px rgba(59, 130, 246, 0.2);
  }

  .capstone__node--transform {
    border-color: rgba(139, 92, 246, 0.3);
  }

  .capstone__node--transform:hover {
    box-shadow: 0 10px 40px rgba(139, 92, 246, 0.2);
  }

  .capstone__node--report {
    border-color: rgba(16, 185, 129, 0.3);
  }

  .capstone__node--report:hover {
    box-shadow: 0 10px 40px rgba(16, 185, 129, 0.2);
  }

  .capstone__node-icon {
    width: 2.5rem;
    height: 2.5rem;
    margin-bottom: 0.75rem;
    color: var(--color-text-primary);
  }

  .capstone__node--extract .capstone__node-icon {
    color: var(--color-day-1);
  }

  .capstone__node--transform .capstone__node-icon {
    color: var(--color-day-3);
  }

  .capstone__node--report .capstone__node-icon {
    color: var(--color-day-4);
  }

  .capstone__node-label {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: 0.25rem;
  }

  .capstone__node-desc {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .capstone__arrow {
    width: 3rem;
    color: var(--color-text-muted);
    transform: rotate(90deg);
  }

  @media (min-width: 768px) {
    .capstone__arrow {
      transform: none;
    }
  }

  .capstone__features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    padding-top: 2rem;
    border-top: var(--border-subtle);
  }

  .capstone__feature {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  .capstone__feature-icon {
    flex-shrink: 0;
    width: 1.25rem;
    height: 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(16, 185, 129, 0.1);
    color: var(--color-accent-success);
    border-radius: 50%;
    font-size: 0.625rem;
  }

  /* ============================================
     PREREQUISITES SECTION
     ============================================ */
  .prerequisites {
    padding: var(--space-section) var(--space-content);
    background: var(--color-bg-secondary);
  }

  .prerequisites__container {
    max-width: 800px;
    margin: 0 auto;
  }

  .prerequisites__grid {
    display: grid;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .prereq-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.25rem;
    background: var(--color-bg-primary);
    border: var(--border-subtle);
    border-radius: var(--radius-lg);
    opacity: 0;
    animation: fade-up 0.5s ease forwards;
    animation-delay: calc(var(--index) * 0.08s);
  }

  .prereq-item__check {
    flex-shrink: 0;
    width: 1.5rem;
    height: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(16, 185, 129, 0.1);
    border-radius: 50%;
    color: var(--color-accent-success);
  }

  .prereq-item__check svg {
    width: 0.875rem;
    height: 0.875rem;
  }

  .prereq-item__text {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
  }

  .prerequisites__note {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    background: rgba(59, 130, 246, 0.05);
    border: 1px solid rgba(59, 130, 246, 0.1);
    border-radius: var(--radius-lg);
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  .prerequisites__note svg {
    flex-shrink: 0;
    width: 1.25rem;
    height: 1.25rem;
    color: var(--color-accent-primary);
  }

  /* ============================================
     DAYS GRID SECTION
     ============================================ */
  .days-grid {
    padding: var(--space-section) var(--space-content);
  }

  .days-grid__container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .days-grid__cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  @media (min-width: 1024px) {
    .days-grid__cards {
      grid-template-columns: repeat(3, 1fr);
    }

    .days-grid__cards > :nth-child(4),
    .days-grid__cards > :nth-child(5) {
      grid-column: span 1;
    }

    .days-grid__cards > :nth-child(4) {
      margin-left: auto;
      width: calc(100% + 0.75rem);
      transform: translateX(calc(25% + 0.375rem));
    }

    .days-grid__cards > :nth-child(5) {
      margin-right: auto;
      width: calc(100% + 0.75rem);
      transform: translateX(calc(-25% - 0.375rem));
    }
  }

  /* ============================================
     FINAL CTA SECTION
     ============================================ */
  .final-cta {
    padding: var(--space-section) var(--space-content);
    background: linear-gradient(180deg, var(--color-bg-primary) 0%, var(--color-bg-secondary) 100%);
  }

  .final-cta__container {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .final-cta__title {
    font-size: clamp(1.5rem, 4vw, 2rem);
    margin-bottom: 1rem;
  }

  .final-cta__desc {
    font-size: 1rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  .final-cta__button {
    position: relative;
    display: inline-flex;
    align-items: center;
    padding: 1.25rem 2.5rem;
    background: linear-gradient(135deg, var(--color-accent-primary) 0%, #2563eb 100%);
    color: white;
    font-size: 1.0625rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: var(--radius-xl);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .final-cta__button:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 50px rgba(59, 130, 246, 0.4);
  }

  .final-cta__button-glow {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s ease;
  }

  .final-cta__button:hover .final-cta__button-glow {
    transform: translateX(100%);
  }

  .final-cta__button-text {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .final-cta__button-text svg {
    width: 1.25rem;
    height: 1.25rem;
    transition: transform 0.3s ease;
  }

  .final-cta__button:hover .final-cta__button-text svg {
    transform: translateX(4px);
  }

  /* ============================================
     RESPONSIVE ADJUSTMENTS
     ============================================ */
  @media (max-width: 640px) {
    .hero {
      padding-top: 4rem;
    }

    .hero__actions {
      flex-direction: column;
      width: 100%;
    }

    .hero__cta {
      width: 100%;
      justify-content: center;
    }

    .capstone__visual {
      padding: 2rem 1rem;
    }
  }
</style>

<script>
  // Intersection observer for scroll-triggered animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
      }
    });
  }, observerOptions);

  // Observe sections
  document.querySelectorAll('.overview, .capstone, .prerequisites, .days-grid, .final-cta').forEach((section) => {
    observer.observe(section);
  });

  // Update day cards based on progress
  import { getProgress, isDayUnlocked } from '@/lib/progress';

  const progress = getProgress();
  document.querySelectorAll('[data-day]').forEach((card) => {
    const dayNumber = parseInt(card.getAttribute('data-day') || '1');
    if (!isDayUnlocked(dayNumber)) {
      card.classList.add('day-card--locked');
    }
  });
</script>
